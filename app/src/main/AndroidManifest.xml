<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lijia.finalproject">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".signUpActivity"
            android:label="Sign Up"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".welcomePage"
            android:label="Welcome Page"
            android:parentActivityName=".signInActivity" />
        <activity
            android:name=".signInActivity"
            android:label="Sign In"
            android:parentActivityName=".Main2Activity" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".adminProfile" />
        <activity android:name=".adminEditOrRemoveService" />
        <activity android:name=".ServiceProviderPage" />
        <activity android:name=".HomeOwnerPage" />
        <activity android:name=".serviceProviderProfileInfoPage" />
        <activity android:name=".service_provider_list" />
        <activity android:name=".service_provider_add_service_2" />
        <activity android:name=".homeOwnerSearchPage" />
    </application>

</manifest>